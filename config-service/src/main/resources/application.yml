server:
  port: 8888

spring:
  config:
    name: configservice
  application:
    name: configservice
  zipkin:
    baseUrl: http://localhost:9411/
  profiles:
    active: native

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8765/eureka/
