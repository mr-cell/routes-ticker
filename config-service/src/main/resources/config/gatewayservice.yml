server:
  port: 9999

spring:
  zipkin:
    baseUrl: http://localhost:9411/

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8765/eureka/

management:
  endpoint:
    health:
      show-details: always